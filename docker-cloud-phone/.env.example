# Docker Compose 变量（复制为同目录 .env，或：docker compose --env-file ../.env up -d）
# 也可在仓库根目录 .env 后执行: ln -sf ../.env .env

BACKEND_PORT=3000
FRONTEND_PORT=5173
DOCKERHUB_NAMESPACE=yiyifred
IMAGE_TAG=0.13.11
DOCKER_PLATFORMS=linux/amd64,linux/arm64
# 后端持久化：compose 将 ./data 挂载到容器 /data（auth.key、cloud-phone.db）
