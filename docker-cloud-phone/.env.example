# Docker Compose 变量（复制为同目录 .env，或：docker compose --env-file ../.env up -d）
# 也可在仓库根目录 .env 后执行: ln -sf ../.env .env

BACKEND_PORT=3000
FRONTEND_PORT=5173
DOCKERHUB_NAMESPACE=dogfood1
IMAGE_TAG=1.21.4-redroid
DOCKER_PLATFORMS=linux/amd64,linux/arm64
REDROID_IMAGE_TAG=redroid:13.0.0_arm64_only_extcam_rgba
REDROID_HOST_WORKDIR=/root/redroid-extcam
REDROID_CAMERA_VIDEO_NR=20
REDROID_CAMERA_WIDTH=1280
REDROID_CAMERA_HEIGHT=720
REDROID_CAMERA_FPS=30
REDROID_ADB_PORT_BASE=5555
# 后端持久化：compose 将 ./data 挂载到容器 /data（auth.key、cloud-phone.db）
