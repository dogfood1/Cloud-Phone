Place Harmony uitest agent here (any of):

  uitest_agent_v1.1.9.x86_64_so   — x86_64 模拟器
  uitest_agent_v1.1.0.so            — arm64 真机
  uitest_agent_v1.1.10.so
  agent.so

Or set HARMONY_AGENT_PATH to the full path.
Cast start picks agent by device CPU ABI (x86_64 / arm64).
